# 本地工具：不发布到 crates.io
[package]
name = "xtask"
publish = false
version.workspace = true
edition.workspace = true

[dependencies]
ai-profile = { path = "../crates/ai-profile", features = ["client", "chat"] }
tokio = { version = "1", features = ["full"] }
